alias ll='ls -alF'alias ls='ls --color'
alias ll='ls -ltr'
alias c='clear'
alias la='ls -A'
alias l='ls -CF'

alias vm='vim ~/dotfiles/.vimrc'
alias vz='vim ~/dotfiles/.zshrc'
alias va='vim ~/dotfiles/.zsh_aliases'
alias vl='vim ~/.zsh_local'

alias here="explorer.exe ."
export PATH=/usr/local/texlive/2020/bin/x86_64-linux:$PATH:/home/takuto/myCommands
export INFOPATH=$INFOPATH:/usr/local/texlive/2020/texmf-dist/doc/info
export MANPATH=$MANPATH:/usr/local/texlive/2020/texmf-dist/doc/man

# run peco with C-r
function peco-history-selection() {
    BUFFER=`history -n 1 | tac  | awk '!a[$0]++' | peco`
    CURSOR=$#BUFFER
    zle reset-prompt
}
zle -N peco-history-selection
bindkey '^R' peco-history-selection

alias python="python3"
alias pip="pip3"
alias piplist="pip freeze | grep -v 'pkg-resources' > requirements.txt; less requirements.txt"
alias act="source bin/activate"
venv() {
	python3 -m venv "$@";
	cd "$@";
	source bin/activate;
	pip3 install wheel;
}
cinit() {
    rm -rf build;
    mkdir build;
    cd build;
    cmake ..;
}
# alias setdns="echo \"nameserver `python3 ~/set_configs.py windowsgateway`\" | sudo tee /etc/resolv.conf"
